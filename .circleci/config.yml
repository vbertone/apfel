version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2204:2023.10.1
    steps:
      - checkout
      - run: sudo apt-get update
      - run: sudo apt-get install gfortran g++ gcc
      - run: cmake . && make -j8
      - run: ctest -V
